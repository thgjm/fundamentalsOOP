Перша лабораторна робота.

Перелік 1. Варіант 7. (*****) - 5 зірочок в сумі.

(****) Графи на основі списку суміжності, матриці суміжності (збереження даних у вершинах та ребрах графів). Додавання та видалення вершин/ребер.
Перевірка на зв’язність графу. Визначення відстані між двома вершинами графу.

+* за інші алгоритми на графах - було реалізовано знаходження доповнення графа6; знаходження мінімального кістякового дерева.

Була пророблена робота з файлами (зчитування графів із файлів). В тестуванні був задіяний doctest (тестування в окремому файлі testing).

Перелік 2. Варіант 3. Виконано завдання a,b.

Теорія ймовірностей
a. Випадкові події. Випадкові величини (дискретні та неперервні). Стандартні випадкові події та розподіли. Комбінування випадкових подій та величин.
b. Обчислення ймовірностей подій, математичного сподівання/очікування, дисперсії.

Реалізовані можливості в коді та їх кількість:

1. 13 класів (3 пов'язаних із графами, і 10 з теорією ймовірності).
2. 27 полів сумовано в усіх класах (хоча в трьох різних реалізаціях графів поля, що означають одне і те саме (наприклад вершини, ім'я файлу) дублюються.)
3. ~67 нетривіальних методів/функцій (оскільки три різних реалізації графів мають "однакові" методи (але що виконані по-різному), то виходить так багато).
  Похибка можлива, але умова, що не менше 25, точно виконується.
4. Ієрархії успадкування: MatrixGraph : Graph, IncendentMatrixGraph : Graph. (хоча наразі я не впевнена, чи є доцільним це успадкування чи воно некоректне).
  (Різні типи розподілів) : DiscreteRandomVariable : RandomVariable : RandomEvent.
  (Різні типи розподілів) : ContinuousRandomVariable : RandomVariable : RandomEvent.
  CombinedRandomEvent : RandomEvent.
5. Випадки поліморфізму: статичний - реалізовані 3 шаблонні функції templates; динамічний - використовуються віртуальні функції в класах теорії ймовірності.
